name: .NET Build

on:
  push:
    branches: [ "master", "feature/*" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    strategy:
      matrix:
        configuration: [Release]

    runs-on: ubuntu-latest

    env:
      solution_name: Orleans.Streams.Kafka-build.sln
      # Test_Project_Path: your-test-project-path

    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    # Install the .NET Core workload
    - name: Install .NET Core
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x

    - name: Print Versions
      run: |
        dotnet --version
        dotnet --info
        echo Node Version:
        node -v

    - name: Extract Version
      run: |
        package_version=$(node -p -e "require('./package.json').version")
        echo "package_version=$package_version" >> $GITHUB_ENV

    # Execute all unit tests in the solution
#     - name: Execute unit tests
#       run: dotnet test

    - name: Restore the application
      run: dotnet restore ./"$solution_name" /p:Configuration="$Configuration"
      env:
        Configuration: ${{ matrix.configuration }}

    - name: Create the app package
      run: | 
        echo "$NUGET"
        dotnet pack "$solution_name" /p:PackageVersion=$package_version -o . -c release
        dotnet nuget push ./*.nupkg -k "$NUGET" -s "nuget.org"
